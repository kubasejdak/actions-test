name: Test build action
on: [push]

jobs:
  build:
    name: gcc-9 build
    runs-on: self-hosted
    container: embeddedlinux/gcc:9-20.04

    steps:
      - uses: actions/checkout@v3
      - name: Look around
        run: |
          echo "================"
          whoami
          echo "================"
          pwd
          echo "================"
          ls -al
          echo "================"
          id
          echo "================"
      - name: Check conan
        run: |
          conan --version
          conan install catch2/3.3.2@
